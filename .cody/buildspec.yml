# Build Specification Reference for CodeBuild: https://docs.aws.amazon.com/codebuild/latest/userguide/build-spec-ref.html

version: 0.2

phases:
  install:
    runtime-versions:
      ruby: 2.6
  build:
    commands:
      - uptime
      - .cody/scripts/ssm.sh $ECS_SERVICE $DOCKER_CONTAINER
      - echo done
